namespace PersonalFinance.Api.Models.Enums
{
    public enum CommitmentNature
    {
        Fixed = 1,      // importe prácticamente estable (alquiler, préstamo)
        Variable = 2    // importe variable (nómina, luz, agua)
    }
}
